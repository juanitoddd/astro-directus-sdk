---
import "@/styles/global.css";
import { getLangFromUrl } from '@/lib/i18n';
import { Font } from "astro:assets";
import Menu from "@/components/layout/Menu.astro";
import LanguageSwitcher from "@/components/layout/LanguageSwitcher.astro";
import Footer from "@/components/layout/Footer.astro";

type Props = { title?: string; menuSlug?: string; footerSlug?: string };
const { title = "Gustav Mahler JUGENDORCHESTER", menuSlug = "main", footerSlug = "footer" } = Astro.props as Props;
const currentLang = getLangFromUrl(Astro.url);
---

<!doctype html>
<html lang={currentLang}>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<link
			rel="stylesheet"
			type="text/css"
			href="https://cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.1/src/regular/style.css"
			/>
			<link
			rel="stylesheet"
			type="text/css"
			href="https://cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.1/src/fill/style.css"
			/>
		<meta name="generator" content={Astro.generator} />
		<Font cssVariable="--font-GTEestiProText" preload />
		<!--
			<Font cssVariable="--font-roboto" preload />
			<Font cssVariable="--font-montserrat" preload />
			<Font cssVariable="--font-playfair-display" preload />		
		-->
		<title>{title}</title>
		<script>
		const VISUAL_EDITING_PARAM = 'visual-editing';
		const isVisualEditing = new URLSearchParams(location.search).get(VISUAL_EDITING_PARAM) === 'true';

		const initDirectusVisualEditor = async () => {
			if (!isVisualEditing) return;
			const directusUrl = 'https://cms.uponthe.top';
			try {
				const { apply } = await import('@directus/visual-editing');
				await apply({ directusUrl });
			} catch (e) {
				console.error('Directus visual editor init failed:', e);
			}
		};

		const propagateVisualEditingParam = () => {
			if (!isVisualEditing) return;
			document.querySelectorAll<HTMLAnchorElement>('a[href]').forEach((a) => {
				const href = a.getAttribute('href');
				if (!href || href.startsWith('#') || href.startsWith('mailto:') || href.startsWith('tel:')) return;
				try {
					const url = new URL(href, location.href);
					if (url.origin !== location.origin) return;
					if (url.searchParams.get(VISUAL_EDITING_PARAM) === 'true') return;
					url.searchParams.set(VISUAL_EDITING_PARAM, 'true');
					a.setAttribute('href', url.pathname + url.search + url.hash);
				} catch {
					/* ignore unparseable href */
				}
			});
		};

		const onReady = () => {
			propagateVisualEditingParam();
			initDirectusVisualEditor();
		};

		document.readyState === 'loading'
			? document.addEventListener('DOMContentLoaded', onReady)
			: onReady();
	</script>
	</head>
	<body class="overflow-x-hidden">
		<header class="grid grid-cols-3 items-center px-6 py-4">
			<Menu slug={menuSlug} lang={currentLang} />
			<a href="/" class="text-lg font-bold text-center uppercase font-light">Gustav Mahler <br/> JUGENDORCHESTER</a>
			<LanguageSwitcher />
		</header>
		<main>
			<slot />
		</main>
		<Footer slug={footerSlug} lang={currentLang} />
	</body>	
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}
</style>
